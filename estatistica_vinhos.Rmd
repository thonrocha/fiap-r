---
title: "Conceitos estatisticos para IA"
output:
  html_document:
    fig_height: 8
    fig_width: 12
    highlight: tang
    toc: yes
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE , warning = FALSE , message = FALSE)
```

# Introdução
bla bla

```{r load libraries}
library(corrplot)

```


```{r load data}
vinhos <- read.csv("Base_alpha.csv", sep = ";", row.names = 1)
```

Verificando os nomes das colunas

```{r}
names(vinhos)
```
Verificando o formato das variáveis
```{r}
str(vinhos)
```

Mostrando estatisticas básicas, para começar a entender o padrão dos dados e identificar se existem dados muito discrepantes ou possiveis erros iniciais.
```{r}
summary(vinhos)
```

# Limpeza dos dados e Feature Selection
Dataset com 14 variaveis, sendo que existe uma variável resposta "Quality", indicando a qualidade medida de cada vinho

# Explicando as variaveis
bla bla bla

# Correlação de variaveis
```{r,message=FALSE,warning=FALSE}
corrplot(cor(vinhos))
```


Gerando histogramas para auxiliar o entendimento do aspecto comportamental dos dados
```{r}
par (mfrow=c(2,2))
hist(vinhos$fixedacidity) 
hist(vinhos$volatileacidity) 
hist(vinhos$citricacid) 
hist(vinhos$residualsugar) 
hist(vinhos$chlorides) 
hist(vinhos$freesulfurdioxide) 
hist(vinhos$totalsulfurdioxide) 
hist(vinhos$density) 
hist(vinhos$pH) 
hist(vinhos$sulphates) 
hist(vinhos$alcohol) 
hist(vinhos$quality) 
hist(vinhos$is_red) 



```

